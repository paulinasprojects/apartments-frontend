// import { PropertiesCardData } from "@/lib/data";
// import { useParams } from "react-router-dom"

const PropertyPage = () => {
  // const {url} = useParams();
  // const property = PropertiesCardData.find((item) => item.url === url);

  return (
    <div>PropertyPage</div>
  )
}

export default PropertyPage